#include <iostream>
#include <stdio.h>
#include <math.h>

int main()
{
    double x ;
    scanf_s("%lf", &x);
    double y ;
    (abs(sqrt(2 + pow(x, 5) / pow(x,4) + 5*x)));
    printf_s("%lf", y);
}


