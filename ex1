#include <iostream>
#include <math.h>
#include <studio.h>

int main()
{
    double a;
    scanf_s("%lf", &a);
    double b;
    scanf_s("%lf", &b);
    double y = sqrt((pow(a, 2) + (2 * a * b) + pow(b, 2)));
    printf("%lf", y);
}
